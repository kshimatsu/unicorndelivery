<body>

  <div class="container">
    <h1>Enjoy your meal time</h1>

      <div class="delivery-time-circle">
        <div class="delivery-time-text">
          <span class="get-your-order">Get your order at
          <span class="delivery-time"><%= @delivery_time.strftime "%l:%m %p" %>
        </div>
      </div>

    <div class="intro-and-login">


      <div class="col-md-6">
        <!-- Intro -->
        <h3>Quick - Curated - Healthy</h3>
        <p>Your time is valuable,
        <p>that's why we aim to bring you a special
        <p>selection of healthy food quickly.
        <p>Enjoy what really matters in life

        <div class="explore-button"><a class="btn btn-primary btn-lg" role="button">Learn more</a></p></div>
      </div>

      <div class="col-md-3">
        <div class="wrapper">
          <form class="form-signin">
            <h2 class="form-signin-heading">Sign up or Login</h2>
            <input type="text" class="form-control" name="username" placeholder="Email Address" required="" autofocus="" />
            <input type="password" class="form-control" name="password" placeholder="Password" required=""/>
            <label class="checkbox">
              <input type="checkbox" value="remember-me" id="rememberMe" name="rememberMe"> Remember me
            </label>
            <button class="btn btn-lg btn-primary btn-block" type="submit">Login</button>
          </form>
        </div>
      </div>
    </div>
  </div>



  <!-- Photos of food at the bottom  -->
  <div class="row-of-suggested-food">
  <h2>We have several yummy options for you to try:</h2>
    <div class="row">
        <% [@western_item, @asian_item, @adventurous_item].each do |item| %>
            <div class="col-xs-6 col-md-3 col-lg-3">
                <strong>Try this <span class="cuisine-name"><%= item.cuisine %></span> item:
                    <%= link_to item.name, controller: 'food_item', action: 'show', id: item.id %>

                    <!-- <a href="/food_item/show/<%= @item.id %>"><%= @item.name %></a></strong> -->
                    <p>It comes from <strong><%= item.restaurant_name %></strong> at the smokin' hot price of <%= item.price %>
                    <p>Harry gives it a <%= item.harrys_choice ? "THUMBS UP" : "THUMBS DOWN" %>
                    <p><%= image_tag item.photo_path %>
            </div>
        <% end %>

    </div>





  </div>

</body>